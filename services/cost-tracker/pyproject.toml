[project]
name = "agent-platform-cost-tracker"
version = "0.1.0"
description = "Agent Platform cost-tracker service (Phase 3-4)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
