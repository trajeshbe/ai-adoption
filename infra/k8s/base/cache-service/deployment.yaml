apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache-service
  namespace: agent-platform
  labels:
    app.kubernetes.io/name: cache-service
    app.kubernetes.io/part-of: agent-platform
    app.kubernetes.io/component: cache
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cache-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cache-service
        app.kubernetes.io/part-of: agent-platform
        app.kubernetes.io/component: cache
    spec:
      serviceAccountName: cache-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: cache-service
          image: ghcr.io/agent-platform/cache-service:latest
          ports:
            - containerPort: 8002
              protocol: TCP
              name: http
          envFrom:
            - configMapRef:
                name: cache-service-config
            - secretRef:
                name: cache-service-secrets
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector:4317"
            - name: OTEL_SERVICE_NAME
              value: cache-service
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
