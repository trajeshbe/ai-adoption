apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: agent-platform
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: agent-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
