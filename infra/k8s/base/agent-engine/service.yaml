apiVersion: v1
kind: Service
metadata:
  name: agent-engine
  namespace: agent-platform
  labels:
    app.kubernetes.io/name: agent-engine
    app.kubernetes.io/part-of: agent-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-engine
  ports:
    - port: 8003
      targetPort: http
      protocol: TCP
      name: http
