apiVersion: v1
kind: Service
metadata:
  name: cost-tracker
  namespace: agent-platform
  labels:
    app.kubernetes.io/name: cost-tracker
    app.kubernetes.io/part-of: agent-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cost-tracker
  ports:
    - port: 8004
      targetPort: http
      protocol: TCP
      name: http
